# Distilled from Claude Code auto-memory — 2026-09-04

These notes were written 2026-08-02 → 2026-08-09 as per-session agent memory while building TH Aeonik / TH Slussen.
They are measured typography, renderer and tooling FACTS, not memory about a person, so they belong in the repo's
knowledge, next to `docs/THAI-LATIN-FONT-ENGINEERING.md` (which supersedes several of them — check its §13 open items).

Known drift at move time (verified 2026-09-04): `scripts/setup-word-th-fonts.ps1` no longer exists (deleted eeafb68);
`assets/fonts/aeonik-wolf/` is now `aeonik-web/`; `aeonik-fixed/` was folded into `aeonik/` (20 faces); TH-Slussen ships
as .otf/CFF; the shipped face count is 22 TH Aeonik + 4 TH Slussen. Two files are self-labelled FALSIFIED/SUPERSEDED
(`thai-mark-size-is-the-slack`, `drop-the-bold-slot-to-free-the-ladder`) and are kept for their quotes and method notes.
